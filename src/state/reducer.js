import initState from "./initState";

const reducer = (state = initState, action) => {
  let newState = state;
  switch (action.type) {
    case "":
      break;
  }
  return newState;
};

export default reducer;
